{
  "emeraldwalk.runonsave": {
    "commands": [
      {
        // Run whenever html, css, or js files are saved
        "match": "\\.*",
        "cmd": "chezmoi apply --force -x scripts"

      }
    ]
  },
}